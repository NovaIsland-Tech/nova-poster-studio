# Only these two public values are embedded in the static website.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Optional LOCAL administrative scripts only. Never prefix this with NEXT_PUBLIC_.
SUPABASE_SECRET_KEY=
APP_URL=http://localhost:3000
